version: '3.8'

services:
  dataset-api:
    build: .
    ports:
      - "4001:4001"
    environment:
      - PORT=4001
      - NODE_ENV=development
    volumes:
      - ./src:/app/src
      - ./data:/app/data
